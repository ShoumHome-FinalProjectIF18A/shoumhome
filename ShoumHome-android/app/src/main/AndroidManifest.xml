<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.shoumhome.android">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".AlarmActivity"></activity>
        <activity android:name=".activityHandler.AlarmManagerActivity" />
        <activity android:name=".activityHandler.PilihPenjualActivity" />
        <activity android:name=".activityHandler.IntroActivity" />
        <activity android:name=".activityHandler.DaftarActivity" />
        <activity android:name=".activityHandler.LayarUtamaActivity" />
        <activity android:name=".activityHandler.ArtikelActivity" />
        <activity android:name=".activityHandler.LoginActivity" /> <!-- <service -->
        <!-- android:name=".AlarmService" -->
        <!-- android:enabled="true" -->
        <!-- android:exported="false" -->
        <!-- android:stopWithTask="false" /> -->
        <!-- <receiver -->
        <!-- android:name=".AlarmBroadcastReceiver" -->
        <!-- android:enabled="true" -->
        <!-- android:exported="false"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.TIME_SET" /> -->
        <!-- </intent-filter> -->
        <!-- </receiver> -->
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activityHandler.MapsResultActivity"
            android:icon="@mipmap/icon"
            android:label="@string/title_activity_maps_result">
            <intent-filter>
                <action android:name=".MapsResultActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activityHandler.KajianActivity"
            android:icon="@mipmap/icon"
            android:label="@string/title_activity_maps_result">
            <intent-filter>
                <action android:name=".KajianActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activityHandler.SplashActivity"
            android:icon="@mipmap/icon">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activityHandler.SettingsActivity"
            android:icon="@mipmap/icon"
            android:label="@string/title_activity_settings">
            <intent-filter>
                <action android:name=".SettingsActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activityHandler.MainActivity"
            android:icon="@mipmap/icon"
            android:label="@string/title_activity_main" />
    </application>

</manifest>